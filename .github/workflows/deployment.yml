name: Deployment

on:
  workflow_dispatch:
    inputs:
      deploy_env:
        description: 'target environment'
        required: true
        default: 'dev'

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.deploy_env }}
    steps:
      - name: deploy
        run: echo "HELLO DEPLOY! env ${{ github.event.inputs.deploy_env }} ${{ secrets.secret_1 }}"